export const SIGNOZ_TRACES_ENDPOINT = process.env.SIGNOZ_TRACES_ENDPOINT;
export const LOG_BATCH_SIZE = 100; // Number of logs to batch before sending
export const FLUSH_INTERVAL = 5000; // Flush every 5 seconds
export const EMPTY_TRACE_ID = '';
export const EMPTY_SPAN_ID = '';
